# MZ-Planner : Planning module for Minus Zero E2E AD framework

The module is built to be compatible with various simulation frameworks (tested on SMARTS). The module connects with the perception module. 